
"use client";

import EarnSection from "@/components/EarnSection";

export default function EarnPage() {
    return (
        <div style={{ paddingTop: '4rem', minHeight: '100vh', display: 'flex', flexDirection: 'column', justifyContent: 'center' }}>
            <EarnSection />
        </div>
    );
}
